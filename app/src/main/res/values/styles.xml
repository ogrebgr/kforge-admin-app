<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LargeText" parent="@android:style/TextAppearance.DeviceDefault.Large" />

    <style name="MediumText" parent="@android:style/TextAppearance.DeviceDefault.Medium" />

    <style name="SmallText" parent="@android:style/TextAppearance.DeviceDefault.Small" />

</resources>
